/**
 * Stub de `server-only` para os testes.
 *
 * O módulo real é fornecido pelo bundler do Next.js e existe apenas para
 * quebrar o build caso um módulo de servidor seja importado no cliente.
 * Nos testes (Node), ele não é necessário.
 */
export {};
